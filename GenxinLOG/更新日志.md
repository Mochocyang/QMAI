# 更新日志

## 20260617-2357-发送按钮回到输入框右侧 (v2.2.16)

### ✨ 调整
- AI 会话和 AI 大纲的发送按钮均移回输入框右侧显示
- 底部工具栏仅保留 Dock 控制、深度模式/编辑章节图标、模型选择器
- 清理因此次调整产生的未使用 import 和函数

### 📂 改动文件
- `src/components/chat/chat-panel.tsx`
- `src/components/sources/outline-chat-panel.tsx`

## 20260617-2325-修复会话框模型下拉框被截断 (v2.2.16)

### 🐛 Bug 修复
- 修复 AI 会话底部工具栏中模型选择器下拉框被父容器裁剪隐藏的问题
- 下拉菜单改为通过 `createPortal` 渲染到 `document.body`，并使用 fixed 定位
- 根据按钮在视口中的位置自动计算下拉框显示位置，优先向上展开，空间不足时向下展开
- 窗口 resize 时自动重新计算位置

### 📂 改动文件
- `src/components/chat/chat-model-selector.tsx`

## 20260617-2248-AI 会话深度模式改为图标开关 (v2.2.16)

### ✨ 调整
- 删除 AI 会话底部工具栏的「普通模式」按钮
- 「深度模式」改为开关式图标按钮：开启时走深度模式，关闭时走普通模式，默认开启
- 「深度模式」按钮仅保留 Brain 图标，减少占用宽度
- 「编辑章节」按钮也改为仅保留 FileEdit 图标
- 两个图标按钮均保留悬停 title 提示

### 📂 改动文件
- `src/components/chat/chat-panel.tsx`

## 20260617-2227-AI 大纲输入框工具栏移到底部 (v2.2.16)

### ✨ 调整
- AI 大纲聊天面板的 Dock 控制按钮和发送按钮从输入框上方/右侧移到底部
- 底部工具栏单行显示，发送按钮位于右侧
- 为 `ChatInput` 增加受控 `value`/`onChange` 支持，便于外部放置发送按钮

### 📂 改动文件
- `src/components/chat/chat-input.tsx`
- `src/components/sources/outline-chat-panel.tsx`

## 20260617-2218-AI 会话底部工具栏改为单行 (v2.2.16)

### ✨ 调整
- AI 会话底部工具栏强制单行显示，不再换行
- 删除模型选择器前的「模型」文字标签
- 空间不足时允许横向滚动

### 📂 改动文件
- `src/components/chat/chat-panel.tsx`

## 20260617-2213-AI 会话工具栏移至输入框底部 (v2.2.16)

### ✨ 调整
- 将 AI 会话顶部的「深度思考 / 普通模式 / 编辑章节」和模型选择器移动到输入框底部
- 发送按钮也一并移到底部工具栏，与模型选择器放在同一行
- 输入框右侧不再显示独立的发送按钮

### 📂 改动文件
- `src/components/chat/chat-input.tsx`
- `src/components/chat/chat-panel.tsx`

## 20260617-2159-小说模型默认跟随 AI 会话选中模型 (v2.2.16)

### ✨ 调整
- 小说设置中的写作/审稿/摘要/提取模型默认跟随 AI 会话下拉框当前选中的模型（`aiChatModel`）
- 未选择 AI 会话模型时，才回退到全局主 LLM 模型
- 选择回 AI 会话模型时，自动恢复为跟随默认

### 📂 改动文件
- `src/components/settings/sections/novel-section.tsx`
- `src/lib/novel/model-resolver.ts`

## 20260617-2154-修复小说任务自定义模型调用 (v2.2.16)

### 🐛 Bug 修复
- 修复小说写作/审稿/摘要/提取模型选择自定义模型后调用失败的问题
- `resolveNovelModel` 现在会根据模型 ID 找到所属 provider 配置，重新计算 baseUrl/apiKey/apiMode
- 模型测试与实际生成统一使用正确的 provider 配置，不再误用当前主 LLM 预设

### 📂 改动文件
- `src/lib/novel/model-resolver.ts`

## 20260617-2122-小说模型设置联动 AI 会话模型 (v2.2.16)

### ✨ 新增功能
- 小说设置中的写作/审稿/摘要/提取模型下拉列表与 AI 会话模型列表保持一致
- 各模型默认跟随 AI 会话当前选中的模型
- 用户可单独选择其他模型进行覆盖

### 🐛 Bug 修复
- 删除小说设置中独立的「拉取模型」按钮及其本地 CLI 拉取逻辑

### 📂 改动文件
- `src/components/settings/sections/novel-section.tsx`
- `src/components/chat/chat-model-selector.tsx`（复用）

## 20260617-2106-AI 会话输入框草稿持久化 (v2.2.16)

### 🐛 Bug 修复
- 修复 AI 会话输入框内容在切换界面后丢失的问题
- 输入框草稿现在按会话持久化，切换离开后返回可恢复
- 发送消息后自动清空对应会话草稿

### 📂 改动文件
- `src/components/chat/chat-input.tsx`
- `src/stores/chat-store.ts`

## 20260617-2033-拆书实验标识与大纲排序修复 (v2.2.16)

### ✨ 新增功能
- 拆书作品侧边栏标题改为「拆书作品（测试功能）」
- 标题旁新增「实验」角标，标识该功能为实验功能

### 🐛 Bug 修复
- 修复大纲导入后章节列表不按数字顺序排序的问题（支持第1章/第一章/第八章等混合格式）

### 📂 改动文件
- `src/components/layout/book-analysis-sidebar-panel.tsx`
- `src/components/layout/knowledge-tree.tsx`
- `src/components/layout/book-analysis-sidebar-panel.spec.tsx`

## 20260617-多模型选择与管理功能增强 (v2.2.16)

### ✨ 新增功能
- **自定义模型卡片支持多模型选择与保存（savedModels）**
- **自定义卡片支持推理/thinking 配置**（复用 ReasoningControls 组件）
- **自定义卡片启用/停用开关**，AI 会话同步过滤
- **AI 会话模型选择器按卡片分组显示**，下拉向上展开避免遮挡
- **设置面板 saved-models-manager 拆分为独立组件**

### 🐛 Bug 修复
- 修复 updateCard 中 undefined 覆盖导致 savedModels 丢失（改用 ?? 回退）
- 修复模型列表点击后自动收起的 bug
- 修复自定义卡片 Header 高度与预设卡片不一致（label+button → hint 两行结构）
- 修复选中自定义模型时 provider 被本地 CLI 覆盖的问题

### 📂 改动文件
- `src/components/settings/sections/custom-provider-cards.tsx`（新增）
- `src/components/settings/sections/llm-provider-section.tsx`
- `src/components/settings/sections/saved-models-manager.tsx`（新增）
- `src/stores/wiki-store.ts`
- `src/components/chat/chat-model-selector.tsx`（新增）
- `src/components/chat/chat-panel.tsx`